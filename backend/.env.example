# Puerto del servidor Express
PORT=3000

# --- Variables futuras (aún no usadas por el código) ---
# Base de datos PostgreSQL (se integrará más adelante)
# DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/nombre_db

# Secreto para firmar tokens JWT (se integrará con autenticación)
# JWT_SECRET=tu_secreto_aqui

# API Key de Mapbox (se integrará con el cálculo de rutas)
# MAPBOX_API_KEY=tu_api_key_aqui